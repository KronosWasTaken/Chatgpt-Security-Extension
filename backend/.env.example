# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
# PostgreSQL connection URLs
# Format: postgresql://username:password@host:port/database
# 
# Sync URL for Alembic and synchronous operations
DATABASE_URL=postgresql://user:password@localhost:5432/aicompliance

# Async URL for FastAPI async operations (uses asyncpg driver)
DATABASE_URL_ASYNC=postgresql+asyncpg://user:password@localhost:5432/aicompliance

# ==============================================================================
# SECURITY KEYS (REQUIRED - CHANGE IN PRODUCTION)
# ==============================================================================
# Generate secure keys with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# Secret key for general application security
SECRET_KEY=your-32-character-secret-key-here

# Encryption key for sensitive data encryption
ENCRYPTION_KEY=your-32-character-encryption-key

# JWT authentication secret key
JWT_SECRET_KEY=your-jwt-secret-key

# JWT signing algorithm (HS256 recommended)
JWT_ALGORITHM=HS256

# ==============================================================================
# CORS CONFIGURATION
# ==============================================================================
# Allowed origins for CORS (comma-separated)
# Adjust for production - remove wildcards and add specific domains
# Examples: http://localhost:5173, https://yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,chrome-extension://*,moz-extension://*

# ==============================================================================
# AI SERVICES (REQUIRED)
# ==============================================================================
# Google Gemini API key for prompt analysis and PII detection
# Get your key from: https://aistudio.google.com/apikey
# Free tier available with generous limits
GEMINI_API_KEY=your-gemini-api-key

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================
# Enable debug mode (set to false in production)
DEBUG=true

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Server bind host (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
HOST=0.0.0.0

# Server port
PORT=8000

# ==============================================================================
# FILE UPLOAD SECURITY
# ==============================================================================
# Maximum file upload size in bytes (10MB = 10485760, 50MB = 52428800)
MAX_UPLOAD_BYTES=10485760

# Allowed file extensions (comma-separated, no dots)
# Add or remove extensions based on your security requirements
ALLOWED_EXTS=pdf,jpg,jpeg,png,txt,md,zip,json,log,csv,pfx,p12,pem,key

# Allowed MIME types (comma-separated)
# Must match the extensions above for consistency
ALLOWED_MIME=application/pdf,image/jpeg,image/png,text/plain,application/json,application/zip
